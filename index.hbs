<!DOCTYPE html>
<html>
<head>
  {{{styles}}}
  <script type="text/javascript" src="//use.typekit.net/ivj5ren.js"></script>
  <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
</head>
<body class="{{tone}}">
  <div class="wrapper">
    <div class="legislator-image">
      <div class="image-wrapper">
        {{#if variants}}
          <img src="{{base_url}}{{target.image}}" alt="{{target.last}}">
        {{/if}}
      </div>
      <div class="image-overlay">
        <img class="grade" src="./images/help_avatar.png" alt="test">
        {{!-- <img src="{{base_url}}/images/{{grade}}.png" alt="{{grade}}"> --}}
      </div>
    </div>
    <div class="content-wrapper">
      <div class="content">
        <div class="dear">Dear {{target.title}} {{target.name}}...</div>
        <div class="map-pin">{{grade}}</div>
        <div class="city-state">
          {{{city}}}
          {{#if state}}
            {{#if city}}
              ,
            {{/if}}
          {{/if}}
          {{#if city}}
            {{state}}
          {{else}}
            {{state_full}}
          {{/if}}
        </div>
        <div class="user-info-wrapper">
          <div class="user-info">
            <div class="avatar">
              <div class="avatar-wrapper">
                {{#if avatar}}
                  <span class="avatar" style="background-image: url({{{avatar}}})"></span>
                {{/if}}
              </div>
            </div>
            <div class="signature">
              {{{first}}} {{{last}}}
              <br>
              {{{city}}}
              {{#if state}}
                {{#if city}}
                  ,
                {{/if}}
              {{/if}}
              {{#if city}}
                {{state}}
              {{else}}
                {{state_full}}
              {{/if}}
            </div>
          </div>
        </div>
      </div>
      <div class="sitelink-text">Find your City's grade at www.ridescore.com</div>
    </div>
  </div>
  {{{scripts}}}
</body>
</html>
